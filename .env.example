# Copia este archivo a .env y completa los valores desde Supabase → Settings → API

# Project URL — Settings → API en Supabase
SUPABASE_URL=https://wxubwrjdtylnpvjtogjp.supabase.co

# Secret key (sb_secret_...) — solo en servidor, nunca en el frontend
SUPABASE_SERVICE_ROLE_KEY=

# Opcional
SESSION_SECRET=cambia-esto-en-produccion
